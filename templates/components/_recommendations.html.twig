{% if recommendations.better_deals is not empty %}
    <div class="mt-5">
        <h4>Better Deals in {{ criteria.location }}</h4>
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Type</th>
                        <th>Bedrooms</th>
                        <th>Price</th>
                        <th>Location</th>
                    </tr>
                </thead>
                <tbody>
                    {% for property in recommendations.better_deals %}
                        <tr>
                            <td>{{ property.type|capitalize }}</td>
                            <td>{{ property.bedrooms }}</td>
                            <td>${{ property.price|number_format }}</td>
                            <td>{{ property.location }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endif %}

{% if recommendations.premium is not empty %}
    <div class="mt-5">
        <h4>Premium Options in {{ criteria.location }}</h4>
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Type</th>
                        <th>Bedrooms</th>
                        <th>Price</th>
                        <th>Location</th>
                    </tr>
                </thead>
                <tbody>
                    {% for property in recommendations.premium %}
                        <tr>
                            <td>{{ property.type|capitalize }}</td>
                            <td>{{ property.bedrooms }}</td>
                            <td>${{ property.price|number_format }}</td>
                            <td>{{ property.location }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endif %}

{% if recommendations.nearby is not empty %}
    <div class="mt-5">
        <h4>Recommended Properties</h4>
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Type</th>
                        <th>Bedrooms</th>
                        <th>Price</th>
                        <th>Location</th>
                    </tr>
                </thead>
                <tbody>
                    {% for property in recommendations.nearby %}
                        <tr>
                            <td>{{ property.type|capitalize }}</td>
                            <td>{{ property.bedrooms }}</td>
                            <td>${{ property.price|number_format }}</td>
                            <td>{{ property.location }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endif %}
